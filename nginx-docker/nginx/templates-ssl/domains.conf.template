map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80 default_server;
    server_name _;

    return 444;
}

server {
    listen 443 ssl default_server;
    server_name _;

    ssl_certificate ${GLIMPSE_SSL_CERTIFICATE};
    ssl_certificate_key ${GLIMPSE_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    return 444;
}

server {
    listen 80;
    server_name ${TASK_DEV_DOMAIN} ${TASK_DEV_API_DOMAIN} ${TASK_PROD_DOMAIN} ${TASK_PROD_API_DOMAIN} ${REPORT_ST_DOMAIN} ${REPORT_ST_API_DOMAIN} ${REPORT_DOMAIN} ${REPORT_API_DOMAIN} ${API_RAG_RUNTIME_ST_DOMAIN} ${H_Q1_DOMAIN} ${H_ASSISTANT_PLAYGROUND_DOMAIN} ${EMBED_DOMAIN} ${RERANK_DOMAIN} ${MLX_DOMAIN} ${MLX_CLASSIFY_DOMAIN} ${MLX_VLM_DOMAIN};

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name ${MLX_DOMAIN};

    ssl_certificate ${GLIMPSE_SSL_CERTIFICATE};
    ssl_certificate_key ${GLIMPSE_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${MLX_UPSTREAM};
        include /etc/nginx/snippets/mlx-stream-proxy.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${TASK_DEV_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${TASK_DEV_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${TASK_DEV_API_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${TASK_DEV_API_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${TASK_PROD_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${TASK_PROD_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${TASK_PROD_API_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${TASK_PROD_API_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${REPORT_ST_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${REPORT_ST_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${REPORT_ST_API_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${REPORT_ST_API_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${REPORT_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${REPORT_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${REPORT_API_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${REPORT_API_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${API_RAG_RUNTIME_ST_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${API_RAG_RUNTIME_ST_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${H_Q1_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${H_Q1_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${H_ASSISTANT_PLAYGROUND_DOMAIN};

    ssl_certificate ${HANWHA_SSL_CERTIFICATE};
    ssl_certificate_key ${HANWHA_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${H_ASSISTANT_PLAYGROUND_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${EMBED_DOMAIN};

    ssl_certificate ${GLIMPSE_SSL_CERTIFICATE};
    ssl_certificate_key ${GLIMPSE_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${EMBED_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${RERANK_DOMAIN};

    ssl_certificate ${GLIMPSE_SSL_CERTIFICATE};
    ssl_certificate_key ${GLIMPSE_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${RERANK_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${MLX_CLASSIFY_DOMAIN};

    ssl_certificate ${GLIMPSE_SSL_CERTIFICATE};
    ssl_certificate_key ${GLIMPSE_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${MLX_CLASSIFY_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name ${MLX_VLM_DOMAIN};

    ssl_certificate ${GLIMPSE_SSL_CERTIFICATE};
    ssl_certificate_key ${GLIMPSE_SSL_CERTIFICATE_KEY};
    include /etc/nginx/snippets/ssl.conf;

    client_max_body_size 25m;

    location = /nginx-health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass ${MLX_VLM_UPSTREAM};
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}

include /etc/nginx/generated/*.conf;
